spring:
  application:
    name: core-service

  datasource:
    url: jdbc:postgresql://localhost:5432/smarterp_core
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver